{% extends 'SieRieBundle:Default:rie.html.twig' %}

{% block contentoption%}

    <div class="block-header">
        <h2>CERTIFICADO DE INSTITUTOS TÉCNICOS Y TECNOLÓGICOS</h2>
    </div>

    <ol class="breadcrumb breadcrumb-bg-cyan">
        <li><a href="{{path('principal_web')}}">Inicio</a></li>
        <li class="active">Certificado RIE</li>
    </ol>

    {% for flashMessage in app.session.flashbag.get('mensajeCertificado') %}
        <div class="alert alert-danger text-center">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            {{ flashMessage }}
        </div>
    {% endfor %}

    <!-- Basic Examples -->
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card">
                <div class="header bg-blue-grey">
                    <h2>
                        Lista de Procesos de certificación
                    </h2>
                </div>
                <div class="body">
                    <div class="row clearfix">
                        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 text-right">
                            <a href="{{path('certificado_rie_new')}}" class="btn btn-md bg-orange waves-effect">
                                <i class="material-icons">add_box</i>Nuevo
                            </a>
                        </div>
                    </div>
                    <div class="row clearfix">
                        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                            <table class="table table-bordered table-striped table-hover js-exportable dataTable">
                                <thead>
                                    <tr>
                                        <th>Acciones</th>
                                        <th>No.</th>
                                        <th>Fecha de registro</th>
                                        <th>Fecha corte</th>
                                        <th>Nro. certificado</th>
                                        <th>Observaciones</th>
                                    </tr>
                                </thead>
                                <tfoot>
                                    <tr>
                                        <th>Acciones</th>
                                        <th>No.</th>
                                        <th>Fecha de registro</th>
                                        <th>Fecha corte</th>
                                        <th>Nro. certificado</th>
                                        <th>Observaciones</th>
                                        
                                    </tr>
                                </tfoot>
                                <tbody>
                                    {% for entity in entities %}
                                        <tr>
                                            <td>
                                                <a href="" class="btn btn-default btn-circle waves-effect waves-circle waves-float" data-toggle="tooltip" data-placement="top" title="Certificado">
                                                    <i class="material-icons">content_copy</i>
                                                </a>                                            
                                                <a href="{{path('certificado_rie_edit', {'idCertificado': entity.id})}}" class="btn btn-default btn-circle waves-effect waves-circle waves-float" data-toggle="tooltip" data-placement="top" title="Editar">
                                                    <i class="material-icons">mode_edit</i>
                                                </a>                                            
                                                <a href="{{path('rie_new_subsede', {'idRie': entity.id})}}" class="btn btn-default btn-circle waves-effect waves-circle waves-float" data-toggle="tooltip" data-placement="top" title="Archivo EXCEL">
                                                    <i class="material-icons">explicit</i>
                                                </a>                                            
                                                <a href="{{path('rie_new_subsede', {'idRie': entity.id})}}" class="btn btn-default btn-circle waves-effect waves-circle waves-float" data-toggle="tooltip" data-placement="top" title="Archivo PDF">
                                                    <i class="material-icons">picture_as_pdf</i>
                                                </a>                                            
                                                <!--    
                                                <a href="{{path('rue_download_lista_certificado', {'certificado': entity.id } )}}">
                                                    <img src="{{asset('img/pdf.png') }}" data-1x="{{asset('img/pdf.png')}}" data-2x="{{asset('img/pdf.png')}}" height="40" width="40"   class="hisrc img-responsive" alt="">                                                        
                                                </a>
                                                <a href="{{path('rue_download_lista_certificado_xls', {'certificado': entity.id } )}}">
                                                    <img src="{{asset('img/excel-xls-icon.png') }}" data-1x="{{asset('img/excel-xls-icon.png')}}" data-2x="{{asset('img/excel-xls-icon.png')}}" height="40" width="40"   class="hisrc img-responsive" alt="">                                                        
                                                </a>
                                                <a href="{{path('rue_download_certificado', {'certificado': entity.id } )}}">
                                                    <img src="{{asset('img/pdf.png') }}" data-1x="{{asset('img/pdf.png')}}" data-2x="{{asset('img/pdf.png')}}" height="40" width="40"   class="hisrc img-responsive" alt="">                                                        
                                                </a>
                                                <form action="{{path('certificado_rue_edit')}}" method="post" style="display:inline-block">
                                                    <input type="hidden" name="idCertificado" value="{{entity.id}}">
                                                    <button type="submit" class="btn btn-xs bg-pink waves-effect">
                                                        <i class="material-icons">mode_edit</i> Editar
                                                    </button>
                                                </form>
                                                -->
                                            </td>    
                                            <td>{{ loop.index }}</td>
                                            <td>{% if entity.fechaRegistro %}{{ entity.fechaRegistro|date('Y-m-d') }}{% endif %}</td>
                                            <td>{% if entity.fechaCorte %}{{ entity.fechaCorte|date('Y-m-d') }}{% endif %}</td>
                                            <td>{{ entity.nroCertificadoInicio }}</td>
                                            <td>{{ entity.observacion }}</td>
                                        </tr>
                                    {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- #END# Basic Examples -->
{% endblock %}
