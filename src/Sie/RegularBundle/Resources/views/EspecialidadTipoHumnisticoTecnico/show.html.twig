{% extends 'layoutRegular.html.twig' %}

{% block contentoption%}
    <h1>EspecialidadTipoHumnisticoTecnico</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Especialidad</th>
                <td>{{ entity.especialidad }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('sie_especialidadHumanisticoTecnico') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('sie_especialidadHumanisticoTecnico_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
