{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EstudianteAsignatura list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Ferchalastupdate</th>
                <th>Version</th>
                <th>Revisionid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('nota_main_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.ferchaLastUpdate %}{{ entity.ferchaLastUpdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.version }}</td>
                <td>{{ entity.revisionId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('nota_main_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('nota_main_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('nota_main_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
