{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InfraestructuraH6AmbienteadministrativoInternadoEst list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>N31esinternadoest</th>
                <th>N32distmetrosfemmas</th>
                <th>N34dormitoriotipoid</th>
                <th>Obs</th>
                <th>Fecharegistro</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('infrah6internadoestudiante_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.n31EsInternadoEst }}</td>
                <td>{{ entity.n32DistMetrosFemMas }}</td>
                <td>{{ entity.n34DormitorioTipoId }}</td>
                <td>{{ entity.obs }}</td>
                <td>{% if entity.fecharegistro %}{{ entity.fecharegistro|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('infrah6internadoestudiante_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('infrah6internadoestudiante_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('infrah6internadoestudiante_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
