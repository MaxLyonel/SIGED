{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InfraestructuraH6Ambienteadministrativo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Obs</th>
                <th>Fecharegistro</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('infrah6ambadm_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.obs }}</td>
                <td>{% if entity.fecharegistro %}{{ entity.fecharegistro|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('infrah6ambadm_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('infrah6ambadm_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('infrah6ambadm_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
