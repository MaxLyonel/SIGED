{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OlimModalidadTipo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Modalidad</th>
                <td>{{ entity.modalidad }}</td>
            </tr>
            <tr>
                <th>Observacion</th>
                <td>{{ entity.observacion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('olimmodalidadtipo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('olimmodalidadtipo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
