{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InfraestructuraH5AmbientepedagogicoEquipamiento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>N53cantidad</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('infrah5ambpedagogicoequipamiento_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.n53Cantidad }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('infrah5ambpedagogicoequipamiento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('infrah5ambpedagogicoequipamiento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('infrah5ambpedagogicoequipamiento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
