{% extends 'layoutHerramientaAlternativa.html.twig' %}

{% block contentoption %}
<div class="header-content">
    <h2><i class="glyphicon glyphicon-leaf"></i> Bienvenido/a <span>Usted a ingresado al sistemas academico de educación alternativa.</span></h2>
    <div class="breadcrumb-wrapper hidden-xs">
        <span class="label">Usted esta aquí:</span>
        <ol class="breadcrumb">
            <li>
                <i class="glyphicon glyphicon-leaf"></i>
                    <a href="{{path('principal_web')}}">Bienvenida/o</a>
                <i class="fa fa-angle-right"></i>
            </li>
        </ol>
    </div>
</div>

<div class="body-content animated fadeIn">
    <div class="row">
        <div class="col-lg-3 col-md-3 col-sm-4">
            <div class="panel rounded shadow">
                <div class="panel-body">
                    <div class="inner-all">
                        <ul class="list-unstyled">
                            <li class="text-center">
                                {% if app.session.get('userfoto') is not null %}
                                    <img src="{{asset('uploads/personasfotos/' ~ app.session.get('userfoto'))}}" class="img-circle img-bordered-primary" alt="{{app.session.get('name')}} {{app.session.get('lastname')}}" height="150" width="150">
                                {% else %}
                                    <img src="{{asset('img/photo.png')}}" class="img-circle img-bordered-primary" alt="{{app.session.get('name')}} {{app.session.get('lastname')}}" height="150" width="150">
                                {% endif %}
                            </li>
                            <li class="text-center">
                                <h4 class="text-capitalize">{{app.session.get('name')}} {{app.session.get('lastname')}}</h4>
                                <p class="text-muted text-capitalize">{{app.session.get('cuentauser')}}</p>
                            </li>
                            <li><br/></li>
                            <li>
                                <div class="btn-group-vertical btn-block">
                                    <a href="{{path('sie_usuarios_homepage')}}" class="btn btn-default"><i class="fa fa-cog pull-right"></i>Editar Cuenta - Gestión de Usuarios</a>                                
                                    <a href="{{path('logout')}}" class="btn btn-default"><i class="fa fa-sign-out pull-right"></i>Salir</a>
                                </div>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-9 col-md-9 col-sm-8">
            <div class="panel rounded shadow">
                <div class="panel panel-teal shadow">
                    <div class="panel-heading">
                        <div class="pull-left">
                            <h3 class="panel-title">Notificaciones</h3>
                        </div>
                        <div class="pull-right">
                            <button class="btn btn-sm" data-action="collapse" data-toggle="tooltip" data-placement="top" data-title="Collapse" data-original-title="" title=""><i class="fa fa-angle-up"></i></button>
                        </div>
                        <div class="clearfix"></div>
                    </div>
                    <div class="panel-body no-padding">
                        <ul class="list-group">
                            <li class="list-group-item">
                                <span class="badge badge-danger">Urgente</span>
                                <h5 class="list-group-item-heading text-primary"><strong><span class="glyphicon glyphicon-time"></span> La Paz, 16 de marzo de 2022</strong></h5>
                                <br>
                                <p class="list-group-item-text list-group-item-text-notif text-justify"><i class="fa fa-rss-square text-warning"></i> Informamos a nuestros usuarios que debido a ataques del virus informático Redline Stealer, que recopila información de inicios de sesión, contraseñas, datos de autocompletar, cookies y detalles de tarjetas de crédito de todos los navegadores web basados en Gecko y Chromium, solicitamos tomar las siguientes acciones:
                                </p>
                                <br>
                                <strong>
                                <ul>
                                    <li>Cambie las contraseñas actuales.</li>
                                    <li>Evitar guardar las contraseñas en el navegador.</li>
                                    <li>Escanee su computadora con un antivirus.</li>
                                </ul>
                                </strong>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-9 col-md-9 col-sm-8">
            <div class="profile-cover">
                <div class="cover rounded shadow no-overflow">
                    <div class="inner-cover">
                        <div class="btn-group cover-menu-mobile hidden-lg hidden-md">
                            <button type="button" class="btn btn-theme btn-sm dropdown-toggle" data-toggle="dropdown">
                                <i class="fa fa-bars"></i>
                            </button>
                            <ul class="dropdown-menu pull-right no-border" role="menu">
                                {% if app.session.get('ie_id') != -1 %}
                                    <li class="active"><a href="{{path('herramienta_ceducativa_seleccionar_cea')}}"><i class="fa fa-history"></i> <span>Historial del Centro.</span></a></li>
                                    <li><a href="{{path('herramienta_alter_galeria_fotos_centro')}}"><i class="fa fa-fw fa-photo"></i> <span>Galeria de Fotos</span> <small></small></a></li>
                                    <li><a href="{{path('herramienta_alter_galeria_fotos_centro_editar_foto')}}"><i class="fa fa-pencil"></i> <span>Editar Fotos</span> <small></small></a></li>
                                {% endif %}
                            </ul>
                        </div>
                    </div>
                    {% if app.session.get('ie_id') != -1 %}
                    <div class="col-xs-12 col-sm-9 col-md-9">
                        <a href="{{path('herramienta_alter_galeria_fotos_centro')}}" class="btn btn-lilac" ><i class="fa fa-fw fa-photo"></i> <span>Galeria de Fotos</span></a>
                        <a href="{{path('herramienta_alter_galeria_fotos_centro_editar_foto')}}" class="btn btn-warning" ><i class="fa fa-pencil"></i> <span>Editar Fotos</span></a>
                    </div>
                    {% endif %}
                </div>
            </div>
            <div class="divider">
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
    </script>
{% endblock %}
