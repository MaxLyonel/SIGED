{% extends 'layoutRegular.html.twig' %}

{% block body -%}
    <h1>Estudiante edit</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('estudiante_main') }}">
                Back to the list
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
