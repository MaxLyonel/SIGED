info_students_index:
    path:     /
    defaults: { _controller:SieEspecialBundle:InfoStudents:index }

info_students_see_students:
    path:     /sie/seestudents/
    defaults: { _controller: SieEspecialBundle:InfoStudents:seeStudents }
    options:
        expose: true

info_students_open_inscription:
    path:     /sie/openinscription/
    defaults: { _controller: "SieEspecialBundle:InfoStudents:openInscription"}
    options:
        expose: true

info_students_findstudent:
    path:     /sie/findstudent
    defaults: { _controller: "SieEspecialBundle:InfoStudents:findStudent"}
    options:
        expose: true

info_students_saveinscription:
    path:     /save
    defaults: { _controller: "SieEspecialBundle:InfoStudents:saveInscription"}
    options:
        expose: true

info_students_removeinscription:
    path:     /sie/removeInscription/
    defaults: { _controller: SieEspecialBundle:InfoStudents:removeInscription }
    options:
        expose: true

info_students_cambiar_estado_matricula:
    path:     /sie/cambiarestado/
    defaults: { _controller: SieEspecialBundle:InfoStudents:cambiarEstadoMatricula }
    options:
         expose: true

info_students_guardar_estado_matricula:
    path:     /sie/gardarestado/
    defaults: { _controller: SieEspecialBundle:InfoStudents:guardarEstadoMatricula }
    options:
         expose: true

herramienta_info_estudiante_certificado_discapacidad:
    path:     /certificado/discapacidad
    defaults: { _controller: "SieEspecialBundle:InfoStudents:getCertificadoDiscapacidad" }
    options:
        expose: true

info_students_open_newinscription:
    path:     /sie/newopeninscription/
    defaults: { _controller: "SieEspecialBundle:InfoStudents:openNewInscription"}
    options:
        expose: true   

info_students_open_checksegipstudent:
    path:     /sie/checksegipstudent/
    defaults: { _controller: "SieEspecialBundle:InfoStudents:checksegipstudent"}
    options:
        expose: true  

info_students_doInscription:
    path:     /sie/doInscription/
    defaults: { _controller: "SieEspecialBundle:InfoStudents:doInscription"}
    options:
        expose: true
        
info_students_goOldInscription:
    path:     /sie/goOldInscription/
    defaults: { _controller: "SieEspecialBundle:InfoStudents:goOldInscription"}
    options:
        expose: true